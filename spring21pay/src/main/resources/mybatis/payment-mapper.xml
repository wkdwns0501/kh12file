<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="payment">

	<select id="sequence" resultType="int">
		select payment_seq.nextval from dual
	</select>	
	
	<insert id="save">
		insert into payment(
			payment_no, payment_member, payment_product,
			payment_tid, payment_name, payment_price
		)
		values(
			#{paymentNo}, #{paymentMember}, #{paymentProduct},
			#{paymentTid}, #{paymentName}, #{paymentPrice}
		)
	</insert>
	
	<select id="list" resultType="PaymentDto">
		select * from payment order by payment_no asc
	</select>
	
	<select id="find" resultType="PaymentDto">
		select * from payment where payment_no = #{paymentNo}
	</select>
	
</mapper>